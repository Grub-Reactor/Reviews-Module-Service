import { shallow, mount, render } from 'enzyme';
import React from 'react';
import Review from '../../client/src/components/Review.jsx';

describe('Review Tests', ()=> {
  it('shoudl have', () => {
    let mounted = mount(<Review />);
  })
});